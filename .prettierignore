node_modules
dist
build
coverage
_archive
pnpm-lock.yaml

# Local-only scratch that is not in git. Without this the local gate is
# permanently red on a file CI never sees — and a gate that always fails is a
# gate you stop reading, which is how two red pushes got past me.
.backup-untracked/
.verify/

# Export web di Godot: runtime generato. E i dati esportati per il progetto
# Godot sono generati da tools, non scritti a mano.
godot-build/
godot/data/
# Addon Godot di terze parti: formattarlo riscrive il codice vendorizzato.
godot/addons/
