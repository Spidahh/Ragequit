store-dir=/tmp/pnpm-store
# Required by pnpm v10 for `pnpm deploy` in monorepo workspaces.
# The lockfile must be generated with this flag active — run `pnpm install`
# after any change here so pnpm-lock.yaml stays in sync.
inject-workspace-packages=true
